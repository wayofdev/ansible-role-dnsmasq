---

galaxy_info:
  role_name: dnsmasq
  author: wayofdev
  description: Ansible role installs Dnsmasq on macOS and configures *.docker hostname.
  company: wayofdev

  license: GPL-3.0-only

  min_ansible_version: "2.13"

  platforms:
    - name: macOS
      versions:
        - Monterey
        - Big-Sur

  galaxy_tags:
    - docker
    - traefik
    - networking
    - bind
    - dns
    - dnsmasq
    - osx
    - mac

dependencies: []

# Ensure that custom Ansible plugins and modules included in the main MacOps
# collection are available to roles in other collections.
collections: ["wayofdev.macops"]

...
